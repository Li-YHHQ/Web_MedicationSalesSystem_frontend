# 课程管理系统前端

基于 Vue 3 + TypeScript + Vite 构建的现代化课程管理系统前端应用。

## 技术栈

- **框架**: Vue 3.4+
- **语言**: TypeScript
- **构建工具**: Vite 5.0+
- **路由**: Vue Router 4
- **状态管理**: Pinia 2
- **HTTP客户端**: Axios
- **样式**: CSS3 + CSS Variables

## 项目结构

```
src/
├── api/                    # API接口调用
├── assets/                 # 静态资源
├── components/             # 公共组件
│   ├── common/            # 通用组件
│   ├── course/            # 课程相关组件
│   └── ui/                # UI基础组件
├── composables/           # 组合式函数
├── layouts/               # 布局组件
├── pages/                 # 页面组件
│   ├── course/            # 课程页面
│   └── dashboard/         # 仪表板
├── router/                # 路由配置
├── stores/                # 状态管理
├── types/                 # TypeScript类型定义
├── utils/                 # 工具函数
├── App.vue                # 根组件
└── main.ts                # 应用入口
```

## 功能特性

### 课程管理
- ✅ 课程列表查看
- ✅ 课程详情展示
- ✅ 新增课程
- ✅ 编辑课程
- ✅ 删除课程
- ✅ 课程搜索
- ✅ 课程筛选（按教师、学分、地点）

### 用户界面
- 🎨 现代化设计
- 📱 响应式布局
- 🌙 主题支持
- 🔄 加载状态
- ❌ 错误处理

### 开发体验
- 🔥 热重载
- 📝 TypeScript 支持
- 🛠️ ESLint 配置
- 📦 组件化开发
- 🎯 路由懒加载

## 快速开始

### 环境要求

- Node.js >= 16.0.0
- npm >= 7.0.0 或 yarn >= 1.22.0

### 安装依赖

\`\`\`bash
npm install
\`\`\`

### 开发模式

\`\`\`bash
npm run dev
\`\`\`

### 构建生产版本

\`\`\`bash
npm run build
\`\`\`

### 预览生产版本

\`\`\`bash
npm run preview
\`\`\`

## 环境配置

项目支持多环境配置：

- `.env` - 默认环境变量
- `.env.development` - 开发环境
- `.env.production` - 生产环境

主要配置项：

\`\`\`env
# API地址
VITE_API_BASE_URL=http://localhost:8080

# 应用标题
VITE_APP_TITLE=课程管理系统
\`\`\`

## API 接口

前端与后端通过 RESTful API 进行交互：

### 课程相关接口
- `GET /courses/{id}` - 根据ID查询课程
- `GET /courses/by-teacher` - 根据教师查询课程
- `GET /courses/search` - 课程名模糊查询
- `GET /courses/by-credit` - 根据学分查询课程
- `GET /courses/by-location` - 根据地点查询课程
- `POST /courses` - 新增课程
- `PATCH /courses/{id}/credit` - 修改课程学分
- `PATCH /courses/{id}/location` - 修改上课地点
- `DELETE /courses/{id}` - 删除课程
- `GET /courses/by-code/{courseCode}` - 根据课程编码查询

## 组件说明

### 基础组件
- `BaseButton` - 基础按钮组件
- `BaseInput` - 基础输入框组件
- `BaseModal` - 基础模态框组件

### 业务组件
- `CourseCard` - 课程卡片
- `CourseForm` - 课程表单
- `CourseList` - 课程列表

### 布局组件
- `DefaultLayout` - 默认布局（包含侧边栏）
- `EmptyLayout` - 空白布局

## 状态管理

使用 Pinia 进行状态管理：

- `useCourseStore` - 课程相关状态
- `useUserStore` - 用户相关状态

## 工具函数

- `request.ts` - HTTP 请求封装
- `validation.ts` - 表单验证工具
- `format.ts` - 格式化工具

## 开发规范

### 命名规范
- 组件：PascalCase（如 `CourseCard.vue`）
- 文件：kebab-case（如 `course-list.vue`）
- 变量/函数：camelCase（如 `getCourseById`）
- 常量：UPPER_SNAKE_CASE（如 `API_BASE_URL`）

### 代码规范
- 使用 TypeScript 进行类型检查
- 遵循 Vue 3 Composition API 最佳实践
- 使用 ESLint 进行代码检查
- 组件使用 `<script setup>` 语法

### Git 提交规范
- `feat:` 新功能
- `fix:` 修复bug
- `docs:` 文档更新
- `style:` 代码格式调整
- `refactor:` 代码重构
- `test:` 测试相关
- `chore:` 构建过程或辅助工具的变动

## 部署

### 构建生产版本

\`\`\`bash
npm run build
\`\`\`

构建完成后，`dist` 目录包含了所有生产环境所需的文件。

### 部署到服务器

将 `dist` 目录下的所有文件部署到 Web 服务器即可。

## 浏览器支持

- Chrome >= 87
- Firefox >= 78
- Safari >= 14
- Edge >= 88

## 许可证

MIT License
